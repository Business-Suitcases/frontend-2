import './Button.css'; // Предполагается, что стили определены в этом файле

const Button = () => {
  return (
    <button className="login-button">
      Войти
    </button>
  );
};

export default Button;